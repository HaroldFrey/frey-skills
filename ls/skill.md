---
name: ls
description: 展示当前工作目录下的所有文件和文件夹，类似 Linux 的 ls 命令
---

当用户调用这个 skill 时，执行以下操作：

1. 使用 PowerShell 执行 `Get-ChildItem -Force` 命令，列出当前工作目录下的所有文件和文件夹（包括隐藏文件）
2. 将结果按类型分组展示：先显示文件夹 📁，再显示文件 📄
3. 每个项目显示名称和最后修改时间
